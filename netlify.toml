[build]
command = “echo ‘Processing environment variables…’”
publish = “.”

[build.processing]
skip_processing = false

[build.processing.html]
pretty_urls = true

[[redirects]]
from = “/*”
to = “/index.html”
status = 200

[context.production.environment]
NODE_ENV = “production”

# Build command to replace environment variables in HTML

[build]
command = “””
sed -i ‘s/{{AUTH0_DOMAIN}}/’$AUTH0_DOMAIN’/g’ index.html &&
sed -i ‘s/{{AUTH0_CLIENT_ID}}/’$AUTH0_CLIENT_ID’/g’ index.html
“””